What to do

-- Create a dashboard for the user to create short links.
-- When a user submits a link, it should be stored in the database.
-- When a user clicks on a short link, it should redirect to the original link.
- encode DB id in base 64 for shorter links